name: App::Wollemi

Data::Context::BEM:
    path : dc/
    fallback : true
    Template:
        INCLUDE_PATH: root/

Model::MyModel:
    host : localhost
    port : 27017
    dbname : wollemi
    #username : myuser
    #password : mypass
    #collectionname : preferedcollection
